Grove PATCH-26: register ZEPH and ZANO in the BasicSwap console UI

PATCH-14/16/20 registered both coins with the engine (chainparams, the
coin-client factory, prepare, run.py) and PATCH-14/20 added zephyr to the
Settings page. The console's JavaScript layer and the offers page were left
half-registered: upstream's own placeholder rows carried ZANO in
offers.html / config-manager.js / offers-pricechart.js but nowhere else, and
ZEPH appeared in none of them. Audited file by file against Wownero -- the
CryptoNote coin upstream fully wires -- on 2026-09-04:

    file                                   zano   zephyr   wownero
    ui/page_settings.py                      0       4        3
    templates/settings.html                  0       9        3
    templates/offers.html                    3       0        4
    static/js/modules/config-manager.js      1       0        1
    static/js/pages/offers-pricechart.js     1       0        1
    static/js/modules/coin-manager.js        0       0        1
    static/js/modules/cache-manager.js       0       0        1
    static/js/modules/notification-manager.js 0      0        1
    static/js/pages/amm-tables.js            0       0        2
    static/js/pages/bid-page.js              0       0        1
    static/js/modules/price-manager.js       0       0        2
    static/js/modules/api-manager.js         0       0        1

Consequences before this patch: the Settings page could not render or save
Zano's fee/daemon fields (page_settings.py's POST handler subscripts
`managedaemon_<coin>` unconditionally, so saving raised KeyError -- the same
defect PATCH-20 fixed for zephyr); `CoinManager.getCoinByAnyIdentifier`
returned nothing for either coin, so the wallet page's price fetch skipped
them, the bid page's scriptless-coin tooltips treated ZANO/ZEPH as scripted,
the AMM tables could not map their names, and the offers page had no ZEPH
entry at all.

Each of the twelve files gains its rows beside Wownero's. `Zephyr.png` and
`Zephyr-20.png` (the icons offers.html and coin-manager.js reference, by the
`coin_name()` convention `<Name>.png`) are binary and travel with the applier
as `upstream/patches/assets/`, not inside this text patch.

diff --git a/basicswap/ui/page_settings.py b/basicswap/ui/page_settings.py
--- a/basicswap/ui/page_settings.py
+++ b/basicswap/ui/page_settings.py
@@ -171,7 +171,8 @@
                     data = {"lookups": get_data_entry(form_data, "lookups_" + name)}
                     # PWNDA-PATCH-14: zephyr takes the same settings-form
                     # fields as monero/wownero (fee_priority, managedaemon_*).
-                    if name in ("monero", "wownero", "zephyr"):
+                    # PWNDA-PATCH-26: zano too -- same CryptoNote form shape.
+                    if name in ("monero", "wownero", "zephyr", "zano"):
                         data["fee_priority"] = int(
                             get_data_entry(form_data, "fee_priority_" + name)
                         )
@@ -364,7 +365,8 @@
         )
         # PWNDA-PATCH-14: same tuple as the form-apply handler above --
         # renders the fee_priority / rpchost / rpcport fields for zephyr too.
-        if name in ("monero", "wownero", "zephyr"):
+        # PWNDA-PATCH-26: and for zano.
+        if name in ("monero", "wownero", "zephyr", "zano"):
             chains_formatted[-1]["fee_priority"] = c.get("fee_priority", 0)
             chains_formatted[-1]["manage_wallet_daemon"] = c.get(
                 "manage_wallet_daemon", "Unknown"
diff --git a/basicswap/templates/settings.html b/basicswap/templates/settings.html
--- a/basicswap/templates/settings.html
+++ b/basicswap/templates/settings.html
@@ -140,7 +140,8 @@
                managedaemon_zephyr and fee_priority_zephyr unconditionally via
                get_data_entry (a raw dict subscript), so without zephyr in these three
                template gates, saving Zephyr settings raised KeyError every time. #}
-            {% if c.name in ('wownero', 'monero', 'zephyr') %}
+            {# PWNDA-PATCH-26: zano added to the same three gates, for the same KeyError. #}
+            {% if c.name in ('wownero', 'monero', 'zephyr', 'zano') %}
             <div class="relative">
              <select class="hover:border-blue-500 bg-gray-50 text-gray-900 appearance-none dark:bg-gray-700 dark:text-white border border-gray-300 dark:border-gray-600 dark:placeholder-gray-400 text-sm rounded-lg outline-none focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 focus:ring-1 focus:outline-none" name="managedaemon_{{ c.name }}">
               <option value="true" {% if c.manage_daemon==true %} selected{% endif %}>True</option>
@@ -323,7 +324,8 @@
         {% endif %}
 
         {# PWNDA-PATCH-20: zephyr added -- CryptoNote RPC configuration block. #}
-        {% if c.name in ('wownero', 'monero', 'zephyr') %}
+        {# PWNDA-PATCH-26: zano added. #}
+        {% if c.name in ('wownero', 'monero', 'zephyr', 'zano') %}
         <div class="mb-6">
          <h4 class="text-sm font-medium text-gray-900 dark:text-white mb-4">
           RPC Configuration
@@ -402,7 +404,8 @@
 
            {# PWNDA-PATCH-20: zephyr added -- renders fee_priority_zephyr, which the
               POST handler requires. #}
-           {% if c.name in ('wownero', 'monero', 'zephyr') %}
+           {# PWNDA-PATCH-26: zano added. #}
+           {% if c.name in ('wownero', 'monero', 'zephyr', 'zano') %}
            <div>
             <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Transaction Fee Priority</label>
             <div class="relative">
diff --git a/basicswap/templates/offers.html b/basicswap/templates/offers.html
--- a/basicswap/templates/offers.html
+++ b/basicswap/templates/offers.html
@@ -103,6 +103,7 @@
 <section class="py-4 px-3 flex-wrap overflow-hidden container-to-blur">
  <div class="lg:container mx-auto">
   <div class="flex flex-wrap justify-center lg:justify-start xl:justify-center" id="coin-container">
+{# PWNDA-PATCH-26: ZEPH registered beside ZANO below (Grove carries both as chainclients). A comment cannot sit inside the set-tag's dict literal: Jinja lexes `{#` there as an unexpected char (seen live 2026-09-04). #}
 {% set coin_data = {
     'BTC': {'name': 'Bitcoin', 'symbol': 'BTC', 'image': 'Bitcoin.png', 'show': true},
     'XMR': {'name': 'Monero', 'symbol': 'XMR', 'image': 'Monero.png', 'show': true},
@@ -117,10 +118,11 @@
     'DCR': {'name': 'Decred', 'symbol': 'DCR', 'image': 'Decred.png', 'show': true},
     'NMC': {'name': 'Namecoin', 'symbol': 'NMC', 'image': 'Namecoin.png', 'show': true},
     'ZANO': {'name': 'Zano', 'symbol': 'ZANO', 'image': 'Zano.png', 'show': false},
+    'ZEPH': {'name': 'Zephyr', 'symbol': 'ZEPH', 'image': 'Zephyr.png', 'show': false},
     'WOW': {'name': 'Wownero', 'symbol': 'WOW', 'image': 'Wownero.png', 'show': true}
     }
   %}
-  {% set custom_order = ['BTC', 'ETH', 'XMR', 'PART', 'LTC', 'BCH', 'FIRO', 'PIVX', 'DASH', 'DOGE', 'DCR', 'NMC', 'ZANO', 'WOW'] %}
+  {% set custom_order = ['BTC', 'ETH', 'XMR', 'PART', 'LTC', 'BCH', 'FIRO', 'PIVX', 'DASH', 'DOGE', 'DCR', 'NMC', 'ZANO', 'ZEPH', 'WOW'] %}
 
   {% if enabled_chart_coins is string %}
     {% if enabled_chart_coins == "" %}
@@ -148,7 +150,7 @@
           <div id="{{ coin_symbol.lower() }}-loader" class="absolute inset-0 flex items-center justify-center bg-coolGray-100 dark:bg-gray-500 z-10 hidden">
            <div class="w-16 h-1 bg-blue-500 rounded-full animate-pulse"></div>
           </div>
-          <p class="my-2 text-xl font-bold text-left monospace text-gray-700 dark:text-gray-100" id="{{ coin_symbol.lower() }}-price-usd"> <span id="{{ coin_symbol.lower() }}-price-usd-value" {% if coin_symbol in ['DCR', 'ZANO', 'WOW'] %}style="min-width: 80px;"{% endif %}></span>
+          <p class="my-2 text-xl font-bold text-left monospace text-gray-700 dark:text-gray-100" id="{{ coin_symbol.lower() }}-price-usd"> <span id="{{ coin_symbol.lower() }}-price-usd-value" {% if coin_symbol in ['DCR', 'ZANO', 'ZEPH', 'WOW'] %}style="min-width: 80px;"{% endif %}></span>
           </p>
           <div class="flex items-center text-sm">
            <div class="w-auto">
diff --git a/basicswap/static/js/modules/api-manager.js b/basicswap/static/js/modules/api-manager.js
--- a/basicswap/static/js/modules/api-manager.js
+++ b/basicswap/static/js/modules/api-manager.js
@@ -275,7 +275,7 @@
                         ? window.CoinManager.getAllCoins().map(c => c.symbol).filter(symbol => symbol && symbol.trim() !== '')
                         : (window.config.coins
                             ? window.config.coins.map(c => c.symbol).filter(symbol => symbol && symbol.trim() !== '')
-                            : ['BTC', 'XMR', 'PART', 'BCH', 'PIVX', 'FIRO', 'DASH', 'LTC', 'DOGE', 'DCR', 'NMC', 'WOW']);
+                            : ['BTC', 'XMR', 'PART', 'BCH', 'PIVX', 'FIRO', 'DASH', 'LTC', 'DOGE', 'DCR', 'NMC', 'WOW', 'ZANO', 'ZEPH']); // PWNDA-PATCH-26
 
                     const response = await this.makeRequest('/json/coinvolume', 'POST', {}, {
                         coins: coinSymbols.join(','),
diff --git a/basicswap/static/js/modules/cache-manager.js b/basicswap/static/js/modules/cache-manager.js
--- a/basicswap/static/js/modules/cache-manager.js
+++ b/basicswap/static/js/modules/cache-manager.js
@@ -523,6 +523,8 @@
         'litecoin': 'LTC',
         'monero': 'XMR',
         'wownero': 'WOW',
+        'zano': 'ZANO', // PWNDA-PATCH-26
+        'zephyr': 'ZEPH', // PWNDA-PATCH-26
         'particl': 'PART',
         'pivx': 'PIVX',
         'firo': 'FIRO',
diff --git a/basicswap/static/js/modules/coin-manager.js b/basicswap/static/js/modules/coin-manager.js
--- a/basicswap/static/js/modules/coin-manager.js
+++ b/basicswap/static/js/modules/coin-manager.js
@@ -145,6 +145,34 @@
             usesCoinGecko: true,
             historicalDays: 30,
             icon: 'Wownero.png'
+        },
+        // PWNDA-PATCH-26: the two Grove CryptoNote followers. Without an entry
+        // here CoinManager.getCoinByAnyIdentifier() returns nothing for them,
+        // so every price/icon/symbol lookup on the wallet, offers and bid
+        // pages silently skipped both coins.
+        {
+            symbol: 'ZANO',
+            name: 'zano',
+            displayName: 'Zano',
+            aliases: ['zano'],
+            coingeckoId: 'zano',
+            cryptocompareId: 'ZANO',
+            usesCryptoCompare: false,
+            usesCoinGecko: true,
+            historicalDays: 30,
+            icon: 'Zano.png'
+        },
+        {
+            symbol: 'ZEPH',
+            name: 'zephyr',
+            displayName: 'Zephyr',
+            aliases: ['zeph', 'zephyr'],
+            coingeckoId: 'zephyr',
+            cryptocompareId: 'ZEPH',
+            usesCryptoCompare: false,
+            usesCoinGecko: true,
+            historicalDays: 30,
+            icon: 'Zephyr.png'
         }
     ];
     const symbolToInfo = {};
diff --git a/basicswap/static/js/modules/config-manager.js b/basicswap/static/js/modules/config-manager.js
--- a/basicswap/static/js/modules/config-manager.js
+++ b/basicswap/static/js/modules/config-manager.js
@@ -241,6 +241,7 @@
                 'pivx': { usd: null, btc: null },
                 'monero': { usd: null, btc: null },
                 'zano': { usd: null, btc: null },
+                'zephyr': { usd: null, btc: null }, // PWNDA-PATCH-26
                 'wownero': { usd: null, btc: null },
                 'firo': { usd: null, btc: null }
             };
diff --git a/basicswap/static/js/modules/notification-manager.js b/basicswap/static/js/modules/notification-manager.js
--- a/basicswap/static/js/modules/notification-manager.js
+++ b/basicswap/static/js/modules/notification-manager.js
@@ -277,7 +277,9 @@
       6: 'XMR',
       7: 'PART (Blind)',
       8: 'PART (Anon)',
-      9: 'WOW',
+      9: 'WOW',
+      16: 'ZANO', // PWNDA-PATCH-26 (Coins.ZANO)
+      19: 'ZEPH', // PWNDA-PATCH-26 (Coins.ZEPH)
       11: 'PIVX',
       12: 'DASH',
       13: 'FIRO',
diff --git a/basicswap/static/js/modules/price-manager.js b/basicswap/static/js/modules/price-manager.js
--- a/basicswap/static/js/modules/price-manager.js
+++ b/basicswap/static/js/modules/price-manager.js
@@ -87,7 +87,7 @@
                     ? window.CoinManager.getAllCoins().map(c => c.symbol).filter(symbol => symbol && symbol.trim() !== '')
                     : (window.config.coins
                         ? window.config.coins.map(c => c.symbol).filter(symbol => symbol && symbol.trim() !== '')
-                        : ['BTC', 'XMR', 'PART', 'BCH', 'PIVX', 'FIRO', 'DASH', 'LTC', 'DOGE', 'DCR', 'NMC', 'WOW']);
+                        : ['BTC', 'XMR', 'PART', 'BCH', 'PIVX', 'FIRO', 'DASH', 'LTC', 'DOGE', 'DCR', 'NMC', 'WOW', 'ZANO', 'ZEPH']); // PWNDA-PATCH-26
 
                 if (!coinSymbols.length) {
                     throw new Error('No valid coins configured');
@@ -182,7 +182,7 @@
 
                 const coinNames = window.CoinManager
                     ? window.CoinManager.getAllCoins().map(c => c.name.toLowerCase())
-                    : ['bitcoin', 'bitcoin-cash', 'dash', 'dogecoin', 'decred', 'namecoin', 'litecoin', 'particl', 'pivx', 'monero', 'wownero', 'firo'];
+                    : ['bitcoin', 'bitcoin-cash', 'dash', 'dogecoin', 'decred', 'namecoin', 'litecoin', 'particl', 'pivx', 'monero', 'wownero', 'firo', 'zano', 'zephyr']; // PWNDA-PATCH-26
 
                 coinNames.forEach(coin => {
                     emptyData[coin] = { usd: null, btc: null };
diff --git a/basicswap/static/js/pages/amm-tables.js b/basicswap/static/js/pages/amm-tables.js
--- a/basicswap/static/js/pages/amm-tables.js
+++ b/basicswap/static/js/pages/amm-tables.js
@@ -572,6 +572,9 @@
             'nmc': 'NMC',
             'wownero': 'WOW',
             'wow': 'WOW',
+            'zano': 'ZANO', // PWNDA-PATCH-26
+            'zephyr': 'ZEPH', // PWNDA-PATCH-26
+            'zeph': 'ZEPH',
             'dash': 'DASH',
             'pivx': 'PIVX',
             'firo': 'FIRO',
@@ -585,7 +588,9 @@
             'NMC': 'NMC',
             'XMR': 'XMR',
             'PART': 'PART',
-            'WOW': 'WOW',
+            'WOW': 'WOW',
+            'ZANO': 'ZANO',
+            'ZEPH': 'ZEPH',
             'FIRO': 'FIRO',
             'DASH': 'DASH',
             'PIVX': 'PIVX'
diff --git a/basicswap/static/js/pages/bid-page.js b/basicswap/static/js/pages/bid-page.js
--- a/basicswap/static/js/pages/bid-page.js
+++ b/basicswap/static/js/pages/bid-page.js
@@ -82,7 +82,7 @@
       return staticTooltip;
     }
 
-    const scriptlessCoins = ['XMR', 'WOW'];
+    const scriptlessCoins = ['XMR', 'WOW', 'ZANO', 'ZEPH']; // PWNDA-PATCH-26
     let scriptCoin, scriptlessCoin;
 
     if (scriptlessCoins.includes(this.coinFrom)) {
diff --git a/basicswap/static/js/pages/offers-pricechart.js b/basicswap/static/js/pages/offers-pricechart.js
--- a/basicswap/static/js/pages/offers-pricechart.js
+++ b/basicswap/static/js/pages/offers-pricechart.js
@@ -410,7 +410,8 @@
   },
 
   setActiveContainer: (containerId) => {
-    const containerIds = ['btc', 'xmr', 'part', 'pivx', 'firo', 'dash', 'ltc', 'doge', 'eth', 'dcr', 'nmc', 'zano', 'wow', 'bch'].map(id => `${id}-container`);
+    // PWNDA-PATCH-26: 'zeph' added.
+    const containerIds = ['btc', 'xmr', 'part', 'pivx', 'firo', 'dash', 'ltc', 'doge', 'eth', 'dcr', 'nmc', 'zano', 'zeph', 'wow', 'bch'].map(id => `${id}-container`);
     containerIds.forEach(id => {
       const container = document.getElementById(id);
       if (container) {
